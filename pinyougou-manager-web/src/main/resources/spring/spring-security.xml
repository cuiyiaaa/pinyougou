<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">
	
	<!-- 配置不拦截的资源 -->
	<!-- 根目录下的html直接放行 -->
	<http pattern="/*.html" security="none"/>
	<http pattern="/css/**" security="none"/>
	<http pattern="/img/**" security="none"/>
	<http pattern="/js/**" security="none"/>
	<http pattern="/plugins/**" security="none"/>
	
	
	<!-- 页面的拦截规则  use-expressions是否启动SPEL表达式，true为缺省值-->
	<http use-expressions="false">
		<!--
			/*：当前根目录下的文件，不包括子文件  /**：当前目录下的所有资源
			access：可以访问的角色名称(必须以ROLE_开头)
		  -->
		<intercept-url pattern="/**" access="ROLE_ADMIN"/>
		<!--开启表达登录功能  -->
		<form-login always-use-default-target="true" login-page="/login.html" default-target-url="/admin/index.html" authentication-failure-url="/login.html"/>
		
		<!--关闭跨域 -->
		<csrf disabled="true"/>
		
		<!-- 配置允许在页面使用 ifmare标签引入其他页面 -->
		<headers> 
			<frame-options policy="SAMEORIGIN"/>
		</headers>
                         
		<!-- 会默认生成一个 /logout地址，进行退出登录 -->
		<logout logout-success-url="/login.html" invalidate-session="true"  />
	</http>

	<!-- 认证管理器 -->
	<authentication-manager>
		<authentication-provider>
			<user-service>
				<user name="admin" password="123456" authorities="ROLE_ADMIN"/>
				<user name="user" password="123456" authorities="ROLE_USER"/>
				<user name="cuiyi" password="123456" authorities="ROLE_USER,ROLE_ADMIN"/>
			</user-service>
		</authentication-provider>
	</authentication-manager>
</beans:beans>




















